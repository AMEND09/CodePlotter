# Core dependencies
# tkinter is usually bundled with Python, no need to install separately

# Image processing (optional, but required for bed image warping)
opencv-python>=4.5.0
numpy>=1.19.0
Pillow>=8.0.0

# For building executable (optional)
pyinstaller>=4.5.0